# AE 콘솔 실측 디자인 토큰

출처: https://ae-demo.thinkingdata.io — 2026-08-03 DOM/computed style 직접 측정
기반 프레임워크: **Ant Design** (+ 자사 `tant-next-*` 래퍼, `art-table` 테이블)

## 기본

| 항목 | 값 |
|---|---|
| Primary | `#3C50FF` (rgb 60,80,255) |
| Primary 진한 테두리 | `#3040CC` (포커스 상태) |
| 본문 텍스트 | `#333333` |
| 제목 텍스트 | `#212121` |
| 배경 | `#FFFFFF` (페이지 전체 흰색) |
| 기본 폰트 크기 | 14px |
| 폰트 스택 | `TA-Roboto, "pingfang sc", -apple-system, TA-SourceHanSansSC, system-ui, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif` |

## 레이아웃

| 항목 | 값 |
|---|---|
| 상단 내비 높이 | **56px** (line-height 56px, 배경 흰색) |
| 좌측 사이드바 폭 | **260px** (내부 아이템 236px) |
| 사이드바 아이템 높이 | **40px**, padding `0 12px 0 8px` |
| 사이드바 선택 상태 | 배경 `#E8EAFF`, radius **8px**, 색 `#333`, weight 400 |
| 채널 생성 드로어 | **우측 드로어 992px** (중앙 모달 아님) |

## 컴포넌트

| 컴포넌트 | 스펙 |
|---|---|
| Primary 버튼 | bg `#3C50FF`, 흰 글자, **h 32px**, radius **8px**, padding `5px 16px`, 14px/400, border·shadow 없음 |
| Secondary(취소) | 투명 배경, `#333`, h 32px, radius 8px, 테두리 없음 |
| Input | **h 32px**, radius **8px**, padding `0 8px`, 14px, 포커스 테두리 `#3040CC` |
| 검색 인풋 | w 220px, h 32px, radius 8px |
| Switch | **w 32 × h 16px**, radius 16px, ON `#3C50FF`, OFF `rgba(185,190,207,.5)` |
| 탭(활성) | 14px/**500**, `#333`, 하단 잉크바 `#3C50FF` **높이 4px** |
| 테이블 헤더 | h **48px**, 14px/**500**, `#212121`, 배경 흰색, padding `0 8px` |
| 폼 라벨 | 14px/**500**, `#333`, 필수 표시 빨간 `*` 앞에 |
| 드로어 제목 | **16px**/500, `#212121` |

## 구조 메모

- 라우트: 운영 모듈 = `#/hermes/...` (workbench / activity / task / flow / setting)
- 상단 내비: 대시보드 · 분석 · 사용자 · 운영 · 설정 센터 · 데이터 · 도구
- 운영 드롭다운: 운영 일정(워크스페이스·운영 캠페인) / 사용자 도달(운영 작업·플로우 캔버스) / 기본 설정(운영 설정)
- 운영 설정 사이드바: 채널 · 피로도 제어 · 승인자 · 화이트리스트 · 분석 설정 · 현지화 · 클라이언트 파라미터 · 설정 테이블 관리
- 채널 탭: Webhook 채널 · 메시지 푸시 · 클라이언트 채널 · 서드파티 플랫폼
- 채널 목록 테이블 컬럼: 채널 이름 · 생성자 · 마지막 수정자 · 피로도 제어 · 채널 스위치 · 작업

---

## 운영 작업 생성 — 실측 구조 (2026-08-03 2차 측정)

### 진입 플로우 (내 목업에 없던 단계)
```
운영 작업 목록 → [＋ 작업 생성]
  → ① 모달 「도달 방식 선택」 (Webhook 도달 / 메시지 푸시 / 클라이언트 도달 3개 카드)
      · 채널이 없는 방식은 회색 비활성 + [채널] 바로가기 링크 노출
  → ② 모달 「Webhook 도달 · 도달 채널 선택」 (검색 + 채널 리스트, 이전/확인)
  → ③ 작성 화면 (#/hermes/task/create)
```

### 작성 화면 = 전체화면 + 좌측 스테퍼
- 상단 바: `‹` 뒤로 · 채널 타입 배지(Webhook) · **작업 이름 입력(80자 이내)** · `UTC-8` · [초안 저장] [저장 후 제출]
- 좌측: 세로 스테퍼 6단계 (현재 단계 강조 + 요약값 표시)
- 우측: 각 섹션이 카드로 세로 나열 (스크롤 연동)

### 6개 섹션과 내용
| # | 섹션 | 구성 |
|---|---|---|
| 1 | **푸시 시점** | 푸시 타입 7종 타일 — 시간 지정-1회 / 시간 지정-반복 / 수동 / 트리거-A 완료 / 트리거-A 완료 후 B 완료 / 트리거-A 완료 후 B 미완료 / 트리거-커스텀 · 푸시 시간 · 승인 시간 초과(자동 종료 / 승인 후 바로 푸시) |
| 2 | **타겟 사용자** | 커스텀 그룹 / 기존 토글 · 사용자 충족 조건 · 사용자 행동 조건 · 사용자 제외 조건 (AND) · **예상 인원 + [예측] 버튼** |
| 3 | **푸시 설정** | **A/B 테스트 활성화** 토글 · 도달 채널 · 피로도 제어 · 화이트리스트(멤버 선택) |
| 4 | **푸시 콘텐츠** | 기본 언어 탭 · 템플릿 필드 입력(0/128) · **[테스트 전송]** |
| 5 | **목표 설정** | 주요 목표 · 설명 추가 · **목표 기간**("성공 수신 후 N일 내 아래 이벤트 실행 시 목표 달성") · 구체적인 규칙 · 조건 추가 |
| 6 | **지표 설정** | 푸시 성공 등 지표 선택 |

### 추가 확인 사항
- 작업 이름은 상단 바에 있음(본문 아님)
- 시간대 표시가 상단에 고정(`UTC-8`)
- 저장은 **초안 저장**과 **저장 후 제출** 2단계

---

## 2차 실측 — `web-ta-demo.thinkingdata.cn` (2026-08-05)

타이포그래피와 치수를 재측정한 값입니다. 시뮬레이터에 그대로 반영했습니다.

### 폰트 스택
| 용도 | 스택 |
|---|---|
| 본문 | `TA-Roboto, "pingfang sc", -apple-system, TA-SourceHanSansSC, …` |
| 페이지 제목 | `TA-NotoSansKR` |

본문 기본값은 **14px / `rgb(51,51,51)`**, 배경 흰색입니다.

### 치수 실측표
| 요소 | 크기 | 타이포 | 기타 |
|---|---|---|---|
| 상단 내비 | H56 | 14px / 400 / line-height 56px | `#333` |
| 내비 항목 | — | — | padding `0 12px`, margin-right 12px |
| 사이드바 컨테이너 | W**260** | — | 배경 흰색, 테두리 없음 |
| 사이드바 제목 | W260 H**53** | 14px / 400 | padding `14px 0 14px 12px` |
| 사이드바 항목 | W**236** H**40** | 14px | `#333` |
| 페이지 제목 | H36 | **24px / 500** / line-height 36px | `rgb(33,33,33)`, `TA-NotoSansKR` |
| 탭 (비활성) | H32 | 14px | `rgb(126,127,128)` |
| 탭 (활성) | H32 | 14px / 500 | 잉크바 4px |
| Primary 버튼 | H32 | 14px / **400** | padding `5px 16px`, radius 8px, `rgb(60,80,255)` |
| 검색 박스 | W**216** H32 | 14px | radius 8px (내부 input H30 / radius 2px) |
| 테이블 `th` | H48 | 14px / 500 / `rgb(33,33,33)` | padding **`0 16px`**, 배경 흰색 |
| 테이블 `td` | H71* | 14px / line-height 18px | padding **`10px 16px`**, `#333` |
| 카운터 라벨 | — | 12px / 500 | `#333` |
| 카운터 값 | — | 20px / 500 / line-height 24px | `#333` |

\* `td` 높이 71px는 2줄 콘텐츠(채널명 + 부제) 기준입니다. 1줄이면 59px.

### 1차 실측(`.io`)과 달라진 점
| 항목 | 1차 반영값 | 2차 실측값 |
|---|---|---|
| 페이지 제목 | 24px / **700** | 24px / **500** + `TA-NotoSansKR` |
| 상단 내비 항목 | padding `5px 12px` | line-height 56px, padding `0 12px` |
| 사이드바 제목 | 16px / 500 | 14px / 400 |
| `th` padding | `0 8px 0 16px` | `0 16px` |
| `td` padding | `12px 8px 12px 16px` | `10px 16px` |
| 비활성 탭 색 | `--ink-3` | `#7E7F80` |
| 검색 박스 | W220 | W216 |
| 카운터 값 | 20px / 600 | 20px / 500 |

### 한국어 라벨 차이 (미결)
`.cn`과 `.io`의 번역이 다릅니다. 현재 시뮬레이터는 `.io` 기준입니다.

| `.io` (현재 적용) | `.cn` |
|---|---|
| 사용자 | 유저 |
| 설정 센터 | 구성 센터 |

---

## 실험(Atlas) 화면 구조 실측 — 2026-08-05

`.cn` 콘솔 확인 결과, **실험 영역에는 좌측 사이드바가 없습니다.** 표가 전체 폭을 씁니다.

### 이동 경로
상단 **실험** 드롭다운으로만 이동합니다.

| 그룹 | 항목 |
|---|---|
| 핵심 모듈 | 실험 목록 · 트래픽 레이어 |
| 기본 설정 | 지표 관리 |

**피처 관리는 실험이 아니라 구성 센터(설정 센터) 소속입니다.**
페이지 경로는 `#/atlas/feature/list`(Atlas)이지만 진입 메뉴는 구성 센터이고,
이 페이지에서 활성화되는 상단 메뉴도 **구성 센터**입니다.

### 구성 센터 드롭다운 (그룹 라벨 없는 평면 3항목, 228 × 240)
| 항목 | 경로 |
|---|---|
| 피처 관리 | `#/atlas/feature/list` |
| 시나리오 관리 | `#/hermes/config/items` |
| 채널 관리 | `#/hermes/setting/channel` |

`시나리오 관리`로 들어가면 좌측 사이드바가 있고(구성 항목 · 데이터 분석 · 구성 전략 트리),
페이지 제목은 `구성 항목`, 버튼은 `+ 구성 항목 만들기`입니다.

### 피처 관리 컬럼 (실물)
`피처 파라미터 이름 · 표시 이름 · 상태 · 파라미터 타입 · 기본값 · 값 할당 규칙 · 그룹 · 접속 측 · 비고 · 생성자 · 작업`

- 첫 컬럼은 `Feature 파라미터 이름`이 아니라 **`피처 파라미터 이름`**
- 마지막 정보 컬럼은 `마지막 수정자`가 아니라 **`생성자`**
- 제목 오른쪽에 작은 **보관됨** 링크 (버튼이 아님)

---

## 가이드 내용 점검 결과 — 2026-08-06

### 실험 투어 확장 (72 → 80단계)
초보자 기준으로 **제출 이후가 통째로 비어 있어** 가이드를 끝까지 따라 해도
실험이 실제로 돌지 않는 상태였습니다. 다음 8단계를 추가했습니다.

| 단계 | 채운 공백 |
|---|---|
| 12–13 코드 연동 | 피처를 만들어도 앱·서버 조회 코드가 없으면 실험이 동작하지 않음. 구버전 앱 대기까지 포함 |
| 68 가드레일 지표 추가 | 42단계에서 4가지 지표 역할을 가르치면서 편집 화면엔 핵심·보조만 있던 모순 해소 |
| 69 필요 표본·기간 산정 | "30일"의 근거. 현재값 → MDE → 군당 표본 → 기간 환산 |
| 72 제출 전 배정 검증(QA) | 운영 투어의 3게이트에 대응하는 실험 쪽 검증 절차 |
| 75 실험 시작 | **제출 ≠ 적용.** 실행 대기에서 시작을 눌러야 배정 개시 |
| 79 일시 중지 / 실험 중지 | 중지 판단 기준과 두 동작의 차이 |
| 80 롤백 | 중지는 신규 배정만 멈춤. 기본값 롤백까지 해야 복구 |

단계 순서는 **실행 시작 → 리포트 → 중지 판단 → 롤백**입니다.

### 단계 번호 자동 부여
단계를 삽입해도 번호가 어긋나지 않도록, `TOURS` 정의 직후 제목의 기존 번호를
제거하고 정의 순서대로 다시 매깁니다. 인트로(첫 `center` 단계)만 번호를 갖지 않습니다.
시나리오 투어(webhook/push/client)에서 중간부터 번호가 사라지던 문제도 함께 해소됩니다.

### 도달 퍼널 표기 통일
세 곳에서 4단계 / 5단계 / 단계명이 서로 달랐습니다. 실제 UI 기준으로 통일했습니다.

**① 예약 푸시 → ② 실제 푸시 → ③ 푸시 성공 → ④ 푸시 클릭**
①~③은 플랫폼 자동 집계, **④만 앱이 올리는 커스텀 이벤트**가 필요합니다.
기존 운영 투어 9단계는 "3단계(도달)는 서버가 리포트"라고 안내해
불필요한 서버 개발을 요구하고 있었습니다.

### 용어를 실물 라벨로 통일
`Feature` → `피처` (한국어 문맥 43곳). 실물 콘솔은 `피처 실험`, `피처 파라미터 이름`을 씁니다.
코드 식별자(`vFeature`, `expFeature` 등)는 그대로 두었습니다.

### 그 외
- 실험 투어 1단계의 데모 프로젝트 ID(1196) 제거
- 21단계 "왼쪽 지표 관리" → 상단 드롭다운 (사이드바 제거 반영 누락분)
- 경로 라벨 `실험 · 피처 관리` → `설정 센터 · 피처 관리`
- 설명 보강: Atlas 용어 정의, 운영 A/B와의 차이, 트래픽 레이어 배타의 구체 사례,
  실험 트래픽 × 그룹 배분 = 실제 노출 비율(50%×50%=25%), 95% 유의성 판정 기준,
  `A100` 슬롯 번호, `값 할당 규칙` 열의 의미

### 실험 상세·편집 화면 실물 대조 — 2026-08-06 (완료)
`.cn` 실물 실험 [0016] test의 상세·편집 화면을 확인해 반영했습니다.

**상세 화면 (실물)**
- 헤더: ← 피처실험 배지 · [0016] test · 초안 · 작업 기록 · 편집 — 시뮬레이터와 일치
- 하위 탭 「실험 설정」 1개, 우측 앵커(기본 정보/실험 설정/통계 지표, 16px) — 앵커는 일치
- **통계 지표는 핵심 지표/보조 지표 2행뿐** — 가드레일·관찰 슬롯 없음

**편집 화면 (실물)**
- 좌측: 실험 트래픽 → 오디언스 → 실험 변수 → 그룹이 **플로우 다이어그램**으로 연결
  (시뮬레이터는 카드 나열 — 구조 차이로 기록만 하고 유지)
- 우측 패널: 실험 가설(선택 사항, 0/800) / 종료 조건 / 통계 지표
- 가설 placeholder: "만약 [통제 가능한 변경]이라면, [핵심 유저 동기/제약/인지 메커니즘]에
  영향을 주므로 [정량적 지표]가 [명확한 방향]으로 변화할 것이다." → 반영
- 오디언스 버튼: 추가 / **비우기** (초기화 아님) → 반영
- 종료 조건: "[30] 일 후 실험이 자동으로 종료됩니다" → 반영
- 그룹 설명 placeholder "설명 정보를 입력하세요", 배분 막대(50%/50%), 절대 백분율 체크박스 확인
- **통계 지표 슬롯은 *핵심 지표 / 보조 지표 + ⊕ 추가 뿐** — 가드레일 버튼 없음

이에 따라 시뮬레이터에서 **가드레일 추가 버튼을 제거**하고, 상세 통계 지표를 2행으로
바꿨습니다. 가드레일 개념은 유지하되 "역방향 지표를 보조 지표 슬롯에 추가"하는 실물
방식으로 투어 66단계와 42단계를 재작성했습니다.

### 미해결
- **리포트 화면은 여전히 실물 대조 불가.** 실물 프로젝트의 유일한 실험이 초안 상태라
  리포트를 열 수 없습니다. 수치·레이아웃은 추정입니다.
- ~~편집 화면의 플로우 다이어그램 레이아웃~~ → **재현 완료 (2026-08-06)**.
  좌측 캔버스: 실험 트래픽 → 오디언스 조건 → 실험 변수 노드를 사다리꼴 커넥터로 잇고
  SVG 분기(fork)로 대조군/실험군 카드에 연결. 그룹 카드에 배분 입력·설명 textarea·피처 값,
  하단에 배분 막대(50%/50%)와 절대 백분율 체크박스. 우측 패널: 실험 가설(0/800 카운터) /
  종료 조건 / 통계 지표(핵심·보조 + ⊕ 추가). 투어 앵커 ID는 전부 유지.
- `.io`(사용자·설정 센터) / `.cn`(유저·구성 센터) 용어 선택 미결.
- `m.example` / `m.caution` 46곳은 패널에서 제거된 뒤로 렌더링되지 않는 죽은 코드입니다.
  무해하여 남겨 두었습니다.

---

## 생성 우선 플로우 + 플로우 인덱스 — 2026-08-06

### 생성 우선(empty-first) 플로우
투어 중에는 **생성·저장 버튼을 지나기 전까지 해당 자산이 목록에 없습니다.**
각 투어의 생성 완료 단계에 `creates:` 태그를 달고, 목록 렌더링이
`TOUR.i > 생성단계` 를 넘긴 뒤에만 행을 그립니다.

| 자산 | 생성 완료 단계 | 비어 있을 때 |
|---|---|---|
| 채널 (webhook/push/client) | 각 채널 투어의 저장 단계 | "채널이 없습니다" |
| 운영 작업 showroom 9행 | 투어 중 항상 숨김, 제출 후 승인 중 1행 | "작업이 없습니다" |
| 피처 | 11. 생성 클릭 | "피처가 없습니다" |
| 트래픽 레이어 | 22. 저장 클릭 | "트래픽 레이어가 없습니다" |
| 지표 | 32. 생성 클릭 | "No data" |
| 실험 (카운터 포함 0→1) | 40. 확인 클릭 | "실험이 없습니다" |
| 설정 항목 카드 | 5. 드로어 완료 | "설정 항목이 없습니다" |

자유 탐색(투어 밖)은 기존 예시 데이터를 그대로 보여줍니다.
피처 3단계는 사라진 행 대신 검색창을 앵커로 재지정했습니다.

### 플로우 인덱스
투어 시작 시 좌측 고정 패널(224px)에 전체 단계를 페이즈별로 나열합니다.
- 클릭하면 해당 단계로 즉시 이동 (`tourJump`)
- 현재 단계 하이라이트 + 자동 스크롤
- 본문을 `margin-left:248px`로 밀어 스포트라이트 대상과 겹치지 않음
  (전체 화면 작업 작성기 `.tcreate`도 함께 시프트)
- 1100px 이하에서는 숨김


---

## 테스트 단말 제거 — 2026-08-06

우측 휴대폰 레일(게임/커머스 앱, 잠금화면·알림·동작 기록)을 삭제했습니다.
실물 콘솔에 없는 요소라 재현 충실도를 떨어뜨렸고, 사용자 요청으로 제거.

- 마크업·전용 CSS·가이드 중 고정 표시 규칙 삭제, 셸 그리드 2단으로 단순화
- `phone()`/`log()`는 무해한 스텁으로 유지 (발송·설정 로직의 호출부 보존)
- `mets()`는 작성 화면의 실시간 도달 퍼널(`#fnLive`) 갱신만 수행
- 단말을 가리키던 문구 2곳을 도달 퍼널 기준으로 수정

---

## 스포트라이트 정합성 수정 — 2026-08-06

345단계 전수 측정(스팟 목적지 좌표 vs 대상 rect) 결과 28단계가 어긋나 있었고,
원인 4가지를 수정해 0건으로 만들었습니다.

| 원인 | 증상 | 수정 |
|---|---|---|
| 하단 가이드 카드에 잘린 대상은 스팟을 통째로 숨김 | 모달 푸터(생성·저장), 페이지 하단 카드에서 강조 없음 | 카드 높이만큼 `main`·작성기에 스크롤 여유 추가, 모달은 상단 정렬 + 최대 높이 제한 |
| sticky 상단바 뒤로 스크롤 | 편집 캔버스 우측 패널 필드가 y=0으로 올라가 가려짐 | `html{scroll-padding-top:128px}` |
| body 마진 트랜지션(0.22s) > 스팟 측정(0.12s) | 투어 시작 첫 단계가 왼쪽으로 어긋남 | 트랜지션 제거 |
| 실험 페이즈 칩 라벨 하드코딩 | 카드 상단 칩이 "제출·이력/리포트"로 구식 표기 | 실제 메타(실행 시작/측정·종료)로 교체 |

측정 시 주의: `#spot`은 0.3s 이동 애니메이션이 있어 rect가 아니라
inline style(목적지)로 비교해야 합니다. 삭제된 테스트 단말의 rect 클램프도 정리.

---

## 공식 문서 대조 반영 — 2026-08-06

`docs-v2.thinkingdata.kr` 실험 문서(개요·실험 목록·트래픽 레이어·지표 관리·SDK)와
대조해 다음을 교정·보완했습니다.

### 교정 (기존 서술이 문서와 달랐던 것)
- **일시 중지**: 규칙 중지 + 수집 중단 + **피처 즉시 기본값 회귀**, 중지 사유 필수,
  재개 가능. (기존 "배정만 잠시 멈춤" 서술 폐기)
- **종료**: 영구적, 피처는 다음 우선순위 규칙으로 회귀(별도 롤백 불필요),
  데이터 동결·리포트 열람 유지, **레이어 트래픽 자동 해제**, 보관 가능.
  (기존 "중지 후 기본값으로 롤백" 버튼·서사 폐기)
- 상태 모델: draft → pending → running → paused/ended (+보관).
  실행 버튼 명칭 "실험 실행", 목록 실행 아이콘 경로 병기.
- **SDK 카드를 공식 API로 교체**: TDExperiment 1.0.1
  (TDAnalytics ≥3.3.6 + TDRemoteConfig ≥1.3.0 필수 동반),
  `getValueAsString/Double/Boolean/Json`, `automaticExposureTracking=true` 기본
  (get 시 노출 자동 업로드), 수동 `exposure()`, 서버 SDK 문서는 준비 중.
- 외부 실험: **후속 지원 예정** 명시.
- 분할 단위 4종: #user_id · #account_id · #distinct_id · #device_id.

### 보완 (문서에 있어 추가한 것)
- 리포트: **95% CI 구간값 + P 값** KPI, **실험 결론** 카드, **실험 시간축**
  (생성→제출→실행→진행, 중지·재개 기록) 카드.
- 지표: **신뢰 가능 통계(可置信统计)** — 유저 단위 선집계 후 유저 표본 검정.
  지표명 규칙(소문자 시작·80자).
- 레이어: 종료 시 점유 트래픽 자동 해제.
- 실험 생성 전 확인 4항목(권한·분할 주체·이벤트 수집 선행·가설/기간).
- 공식 권장 순서(피처 → 지표 → 레이어) 병기.

---

## 화면 전환 애니메이션 — 2026-08-06

- 본문(#main)·사이드바: 화면 구성이 실제로 바뀔 때만 0.3s 페이드
  (viewKey 비교 — 같은 화면 재도색에는 재생하지 않음)
- 모달·드로어·작업 작성기·드롭다운·가이드 홈: 열릴 때 0.16~0.3s 페이드
- 투어 시작 시 본문 시프트(margin 0.3s)가 끝난 뒤 첫 스팟을 그리도록
  showStep을 310ms 지연 — 시프트 중 측정으로 어긋나던 문제 방지
- 모든 전환은 opacity만 사용 (transform 없음) — 요소 위치가 변하지 않아
  스포트라이트 측정과 충돌하지 않음
- `prefers-reduced-motion: reduce` 시 전부 비활성화

---

## 가이드 카드 → 앵커형 말풍선 — 2026-08-06

하단 도킹 카드가 시선에서 멀다는 피드백으로, 가이드를 **강조 요소 바로 아래
붙는 말풍선**으로 교체했습니다.

- 구성: STEP n/N · 페이즈 / 단계 제목 / **핵심 역할 한 줄** / 이전·다음
  (개발팀·사업팀 상세, 경로, 진행 바 등은 제거 — 좌측 플로우 인덱스가 맥락 담당)
- 배치: 강조 영역 아래 12px, 공간이 없으면 위로. 화살표가 강조 요소 중심을 가리킴
- 인트로(center) 단계는 화면 중앙 표시
- 스팟이 요소를 30% 이상 가리면 검출하는 회귀 테스트 추가 — 185+80단계 0건
- rectOf에서 하단 카드 클램프·도킹 분기 제거, scrollIntoView는 항상 center
- 구형 드래그·도킹·크기조절 코드는 참조가 끊긴 상태로 잔존 (무해)

---

## 튜토리얼 시작 화면 — 2026-08-06

시뮬레이터를 튜토리얼 중심으로 재구성했습니다.

- **로드 즉시 선택 화면**: 8개 튜토리얼 카드(운영 4 · 설정 센터 1 · 실험 3)를
  그룹별로 나열, 카드 클릭 한 번으로 바로 시작
- **투어 종료 후 자동 복귀**: 가이드가 끝나거나 ✕로 닫으면 선택 화면으로 돌아옴
- 우측 하단 작은 '💡 사용 설명' 메뉴 버튼 → **큰 알약형 「▶ 튜토리얼 시작」
  버튼(48px)** 으로 교체. ✕로 닫고 자유 탐색 중에도 항상 노출
- 기존 중간 홈 화면(운영 모듈 한눈에 보기 등)은 경로에서 제외 (코드는 잔존)

### 전수 검증 (8개 투어 · 345단계)
앵커 누락 / 스팟 숨김·이탈 / 말풍선 화면 이탈 / 말풍선의 요소 가림 /
시작 화면 잔존 — **모두 0건**. 카드 클릭 시작·종료 후 복귀·버튼 재오픈 동작 확인.

---

## 시작 화면 2단계화 + 개발팀 작업 표시 — 2026-08-06

### 시작 화면
카드 8개 일괄 노출 → **모듈 3버튼(운영 모듈 / 설정 센터 / 실험) 먼저**,
클릭하면 해당 모듈의 튜토리얼 리스트로 전개. ‹ 모듈 선택으로 복귀.

### 개발팀 코드 작업 표시 (devCode 플래그, 18단계)
개발 코드 작업이 필요한 단계에 두 가지 표시:
- 말풍선: 주황 박스 「⚙ 개발팀 코드 작업 필요」 + 해당 단계의 개발 할 일 한 줄
- 플로우 인덱스: 단계명 앞 ⚙ 마크

| 투어 | 표시 단계 |
|---|---|
| 운영/Webhook | 5 URL(수신 서버) · 7 채널 검증(서명) · 8 암호화(검증 로직) · 9 도달 퍼널(클릭 이벤트) · 10 템플릿(파싱) |
| 메시지 푸시 | 5 토큰 속성(SDK 리포트) · 7 도달 퍼널 |
| Client 푸시 | 4 메시지 타입(렌더링 분기) · 6 노출·클릭 퍼널 |
| 설정 센터 | 4 항목 ID(조회 코드) · 6 파라미터(폴백) · 9 사용 설정(SDK 연동) · 11 Webhook 채널(엔드포인트) |
| 실험 | 5 파라미터 이름(조회 키) · 8 기본값(폴백) · 12–13 코드 연동·배포 · 72 QA 검증 |

### 실주행에서 찾은 버그
'다음' 버튼 클릭이 document로 버블링돼 전역 핸들러가 투어가 연 드롭다운을
닫던 문제 — 가이드 UI(#tourcard·#tourindex) 클릭은 예외 처리.
게임 밸런스 실험 80단계 실주행(실클릭) 무결점 완주로 확인.

---

## 부드러운 스크롤 + 운영·설정 센터 실주행 검증 — 2026-08-06

### 전환 개선
- 단계 이동 시 순간이동하던 스크롤을 **부드러운 스크롤**로 교체.
  도착 좌표를 미리 계산해 스팟·말풍선을 최종 위치로 곧장 그리므로,
  페이지와 스팟이 함께 미끄러져 동시에 도착합니다.
  - 내부 스크롤 컨테이너(드로어 본문 등)는 컨테이너 기준으로 동일 계산
  - fixed/sticky 대상(편집바·모달 푸터)은 스크롤 생략
- 본문·드로어 전환에 8~14px 상승 모션 추가 (300~320ms)
- 말풍선 이동에 0.25s 트랜지션
- **2차 보정 페인트(560ms)**: 데모 타이핑·체크 펼침 등 늦은 레이아웃
  변화가 스팟을 어긋나게 하면 트랜지션으로 스르륵 재정렬

### 실주행 검증 (실제 '다음' 클릭, 700ms 간격)
운영 전체 24 · Webhook 23 · 메시지 푸시 19 · Client 푸시 19 · 설정 센터 20
— 전부 완주, 시작 화면 복귀, 이슈 0건.

발견·수정한 버그:
- **Client 드로어의 컨텐츠 템플릿 라벨에 f.tpl 도움말 마커 부재** →
  8단계 스팟이 항상 사라졌음. 기존 프로그램 스윕은 대상 null을
  건너뛰어 못 잡던 맹점 — 스윕도 숨김 우선 검사로 강화.
- 운영 22단계가 위저드 늦은 렌더로 어긋나던 문제 → 2차 보정 페인트로 해소.

---

## 구성 센터 실물 재구성 — 2026-08-06 (.cn 재확인)

`web-ta-demo.thinkingdata.cn` 구성 센터를 다시 열어 화면 단위로 재구성했습니다.

### 용어를 실물 콘솔 표기로 전환 (82곳)
설정 센터→**구성 센터** · 설정 항목→**구성 항목** · 설정 전략→**구성 전략** ·
설정 템플릿→**구성 템플릿** · 설정 채널→**구성 채널**
('설정 파일', '사용 설정', '분석 설정' 등 실물이 쓰는 표현은 유지)

### 구성 항목 리스트 (실물 구조)
- 제목 옆 파란 「📍 사용 설명」 칩, 검색 "구성 항목 이름/ID 검색", 「＋ 구성 항목 만들기」
- 중앙 정렬 히어로: "차세대 게임 운영 및 수익화 엔진" 24px/500 + 부제 + 접기
- 4단계 일러스트 카드(74px 아이콘)가 **점선 화살표로 연결**:
  설정 시나리오 확인(＋구성 항목 만들기) → 연동 설정(채널 설정→) →
  운영 전략 만들기(더 알아보기→) → 전략 분석(더 알아보기→) — 실물 문구 그대로

### 사이드바 2변형 (실물)
- 시나리오 관리: 구성 항목 / 데이터 분석 + 「구성 전략」 라벨(⌕) + 전략 트리
- 채널 관리: Webhook 채널 / 클라이언트 채널 (구성 채널 화면 진입 시)

### 채널 관리 화면 (실물)
제목 Webhook 채널/클라이언트 채널 + 채널 생성 + **접이식 2단계 가이드 배너**
(실물 문구 그대로: 경량 개발 연동/채널 새로 만들기 · AE SDK 연동/채널 새로 만들기)
+ 테이블(채널 이름·생성자·마지막 수정자·채널 스위치·작업) + No data

### 데이터 분석 화면 (실물)
구성 항목 선택 셀렉트 + 빈 상태("데이터 없음 / 현재 구성 항목에 실행된 전략이
없습니다.") — 기존 이벤트 매핑 표는 가이드 전용 팁으로 대체

### 구성 항목 만들기 모달 (실물)
중앙 460px: 이름(80자) / ID ⓘ / 비즈니스 타입 라디오(설정 파일·독립 파라미터) /
아이콘(색상 OR 이미지 업로드) / 그룹 / 비고 0/200 — 취소·저장

### 함께 수정
120ms 스팟 페인트 타이머에 종료 가드가 없어 endTour 직후 말풍선이
되살아나던 버그 수정.

---

## 구성 센터 투어 실물 흐름 보강 — 2026-08-06

5단계(모달) → 파라미터 화면으로 순간이동하던 구간에 실물 흐름 3단계 삽입
(구성 센터 투어 20 → 23단계):

6. **'저장' 클릭** — 모달 저장 (creates 게이트가 이 단계로 이동:
   저장 전에는 목록·사이드바 트리에 항목 없음)
7. **생성된 구성 항목 카드 클릭** — 목록에 나타난 카드로 상세 진입
8. **'파라미터 관리' 탭 클릭** — 상세 5탭(기본 정보/템플릿/파라미터/사용/분석)
   구조 소개 후 탭 이동 (탭에 ctab-* 앵커 부여)

---

## 파라미터 생성 흐름 추가 — 2026-08-06

구성 센터 투어에서 파라미터도 '보여주기'가 아니라 '만들기'로 (23 → 26단계):

9. **'파라미터 새로 만들기' 클릭** — 빈 목록에서 생성 창 열기
10. **파라미터 이름·타입 입력** — 코드 변수명과 동일해야 함(devCode 배지),
    STRING/NUMBER/BOOLEAN/JSON + 입력 방식, banner_title 자동 타이핑 데모
11. **힌트 문구·필수 여부** — 운영자 안내와 필수 지정의 의미
12. **'저장' 클릭** — creates 게이트. 저장 전에는 목록 No data,
    저장 후 행 등장 + 템플릿 연결 게이트(cfgReady.param) 자동 준비

파라미터 모달(cfgparam)은 중앙 460px, 실물 컬럼(이름/타입/힌트/필수)과 일치.
자유 탐색에서도 버튼 → 모달 → 저장으로 실제 행이 생기도록 연결.

---

## 구성 전략 생성 흐름 + 전수조사 — 2026-08-06

공식 配置策略/配置模版 문서를 정독해 전략 구간을 '소개 5단계'에서
**실제 생성 흐름 11단계**로 재구성했습니다 (구성 센터 26 → 32단계 + 인트로).

### 전략 관리 화면 (문서 실물 구조)
- 진입: 사이드바 구성 전략 트리에서 항목 클릭 (실물 경로, #stratTreeItem)
- 목록: 전략 ID/이름/템플릿/상태/게시 시간/종료 시간/작업(게시·일시 중지·오프라인)
- 생성 게이트: 저장 전 No data → 저장 후 ST-0001 초안 행
- 상태 흐름: 초안→심사 중→게시 대기→게시됨→일시 중지→오프라인
  (+Webhook: 게시 중/실패 · 클라이언트: 등록 중/실패)

### 새 전략 만들기 모달 (문서 3부 구성)
① 목표 수용자 — 목표 유저(전체/커스텀 세그먼트 + 인원 예측,
   #user_id 주체·시간대 일치 제약) + 목표 환경(채널이 정의한 환경 파라미터)
② 전략 내용 — 개통 템플릿만 선택 가능 + 파라미터 값 입력(필수 검증)
③ 발표 설정 — 게시/종료 시간, 전략 표시 시간(스케줄 표시용, 단말 미전송),
   Webhook은 종료 지령 기본 미전송 주석 + 발송 테스트(전체 링크)

### 투어 신규 단계 (19~30)
트리 진입 → 만들기(전제: 개통 채널 바인딩+개통 템플릿) → 목표 유저 →
인원 예측 → 목표 환경 → 템플릿 선택 → 값 입력 → 발표 설정 → 발송 테스트 →
저장(전략 ID 생성) → 상태 흐름·새 버전 규칙 → 일시 중지 vs 오프라인(권한)

### 함께 수정
- rectOf가 24×18 미만 대상을 버리던 것을 최소 크기 확장으로 변경 —
  이때 **DOMRect가 읽기 전용**이라 확장이 무시되던 버그까지 수정(객체 복사)
- 직역 용어 정규화 39곳 (상선→게시, 하선→오프라인, 인군→세그먼트 등)

### 전수조사 결과
구성 센터 33단계 실주행(실클릭) 완주 — 문제 0 · 정상 종료 · 시작 화면 복귀.
8개 투어 전체 스윕(앵커/스팟/말풍선/배지) 0건.

---

## .io 실환경 대조 — 전략·상세 화면 실물화 (2026-08-06)

`ae-demo.thinkingdata.io` 프로젝트 3(로그인 세션)에서 미확인이던 3개 영역을
실물로 열어 대조·재구성했습니다. 실환경 조작은 조회와 모달·위저드 열기까지만
(저장·제출·토글 없음).

### 확인된 .io 실물 (한국어 표기: 설정 항목/설정 센터/설정 전략!)
- 리스트: 폴더 그룹 카드(New Players/기본 폴더), 카드에 채널명 표시
- 상세: 전체 화면 + 5탭, 템플릿 목록(ID/이름/비고/생성자/수정자/상태 토글/작업)
- 파라미터 모달: *이름/*표시 이름/*타입(결합: 문자열/한 줄 텍스트)/기본값/
  힌트 ⓘ/*필수 여부(셀렉트) — 기존 시뮬에 기본값 누락 → 추가
- 분석 설정: 사전 정의 이벤트 3열 + 분석 지표 직접 설정 테이블 → 반영
- 전략 관리: 항목명+채널 배지, 리스트/일정/데이터 토글, 일괄 작업,
  상태 카운터 8종(전체/활성/활성화 대기/일시 중지됨/비활성/초안/승인 중/거부),
  컬럼 10종, 비활성 버튼 사유 툴팁(실물 문구) → 전면 재구성
- 전략 생성: 템플릿 선택 모달 → **풀스크린 3단계 위저드**
  (상단: 채널 배지·이름 인라인 80자·UTC·초안 저장/테스트 전송/저장 후 제출,
  좌: 스텝퍼 3단계, 1 타겟 오디언스[타겟 사용자/대상 환경 카드 + 모든 사용자|
  커스텀 그룹 + 예상 인원], 2 전략 콘텐츠[현재 템플릿·Json 구조·파라미터 0/200],
  3 게시 설정[발행 타입 1회|반복 · 출시(지정/수동) · 종료(지정/영구) ·
  일정 시작 토글]) → 중앙 모달을 위저드로 교체, 투어 재배선

### rectOf 가로 스크롤 보정
넓은 테이블(가로 스크롤) 안의 대상이 뷰포트 밖이면 스팟이 소멸하던 문제 —
가로 컨테이너를 중앙 스크롤하고 도착 좌표 보정.

### 용어 재결정 필요 (보류)
.cn = 구성 센터/구성 항목, **.io = 설정 센터/설정 항목**.
현재 시뮬은 .cn 기준(구성)이나 실사용 .io 환경은 '설정' 표기 —
고객 안내 기준 결정 필요.

---

## 템플릿 생성·변수 연결 흐름 — 2026-08-11 (.io 실조작 확인)

사용자가 띄운 .io 실환경에서 직접 조작하며 파라미터→템플릿 전 과정을 확인
(상점 가격 정책에 banner_title 파라미터·main_banner 템플릿 실제 생성,
변수 연결·개통은 하지 않음 — 실습 자산으로 남음).

### 실물 확인 사실
- 파라미터 저장 후 **모달이 닫히지 않고 유지**(연속 생성 UX), 목록 즉시 갱신.
  필수 여부 옵션은 필수/선택 2종
- 새 템플릿 만들기: 이름(80자)/ID ⓘ/비고(0/200), 버튼은 **「설정으로 이동」**
- 「설정으로 이동」 → **템플릿 변수 편집** 화면:
  토스트("템플릿이 생성되었습니다. 템플릿 변수를 계속 설정해 주세요"),
  좌 캔버스("오른쪽 파라미터를 여기로 드래그하세요"), 우 「선택적 변수 0/N」
  패널(검색·＋·파라미터 카드, 툴팁 "클릭하거나 왼쪽으로 드래그하여 추가"),
  Json 구조 버튼(다운로드), 우상단 취소/저장
- 개통(활성) 상태 템플릿은 편집 불가 — 툴팁
  "템플릿이 활성화 중이므로 편집할 수 없습니다"

### 시뮬레이터 반영 (구성 센터 33 → 38단계)
13 '템플릿 관리' 탭 클릭 → 14 '새 템플릿 만들기' → 15 이름·ID(devCode) →
16 '설정으로 이동'(creates:cfgtpl, 생성 전 목록 No data) →
17 템플릿 변수 연결(카드 클릭 시 캔버스에 폼 등장, 0/1→1/1) →
18 변수 저장 → 19 활성화 게이트(기존).
템플릿 변수 편집 화면(tpledit) 신설 — 실물 구성 그대로.
개통 중 편집 불가 툴팁을 목록 ✎에 반영. 전략 위저드·목록 표기 main_banner 연동.

---

## 구성 센터 2회 검수 — 건너뛰던 프로세스 5곳 보완 (2026-08-11)

### 1차 검수에서 찾은 점프 → 채움 (38 → 42단계)
| 빠졌던 프로세스 | 새 단계 |
|---|---|
| 템플릿 발송 테스트 없이 개통 설명만 | 19. 발송 테스트(작업열 ➤) — 개통 전 필수 |
| 상태 토글을 실제로 켜지 않음 | 20. 상태 토글 개통(스위치 클릭, 개통 중 편집 불가 규칙 포함) |
| 사용 설정 탭 텔레포트 | 21. '사용 설정' 탭 클릭 |
| 연동 버튼 안 누르고 채널 페이지 점프 | 22. '연동' 클릭 — 채널 페이지로 |
| 채널 드로어 저장 없이 다음으로 | 26. 채널 '저장' 클릭 (creates:cfgchan — 저장 전 No data) |
| 전략 제출 후 게시 실행 없음 | 38. '게시' 클릭 (행 ▶ — 상태 초안→활성, 게시 시각 표시) |

기존 '활성화 게이트' 설명 단계는 토글 단계에 흡수. 상태 동기화를
creates/단계 통과 기반으로 정리(template=개통 후, channel=저장 후,
strategy=제출 후 draft→게시 후 online).

### 2차 검수 (실주행)
42단계 실클릭 완주 — 문제 0 · 정상 종료 · 시작 화면 복귀 · JS 에러 0.
게이트 전이(채널 저장 전/후, 개통 전/후, 게시 후 행 활성) 확인.
나머지 5개 투어 회귀 스윕 0건.
